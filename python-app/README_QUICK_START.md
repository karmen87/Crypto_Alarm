# Quick Start Guide

## Get Up and Running in 3 Steps

### Step 1: Install Python

Make sure you have **Python 3.8+** installed:
```bash
python3 --version
```

If not installed, download from: https://www.python.org/downloads/

### Step 2: Run the Startup Script

**On Linux/Mac:**
```bash
./start.sh
```

**On Windows:**
```bash
start.bat
```

The script will automatically:
- Create a virtual environment
- Install all dependencies
- Start the Flask server

### Step 3: Open Your Browser

Navigate to: **http://localhost:5000**

That's it! You're ready to monitor crypto prices.

---

## First Time Usage

1. **Add a trading pair**: Type `BTCUSDT` and click "Add Pair"
2. **Set an alarm**: Click "Alarms" → Enter target price → Click "Add"
3. **Test the alarm**: Click the "🔔 Test Alarm" button to verify sound works

---

## Stopping the App

Press `Ctrl+C` in the terminal where the app is running.

---

## Need More Info?

See the full documentation: [PYTHON_SETUP.md](PYTHON_SETUP.md)
